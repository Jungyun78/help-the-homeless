days = read.csv("days.csv")
days
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 0:len(days)) {
for (j in 0:len(list_header)) {
if days[i:i+1, list_header[j]] == 0{
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 0:len(days)) {
for (j in 0:len(list_header)) {
if (days[i, list_header[j]] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed.append(consecutive_count)
consecutive_count = 0
}
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 0:length(days)) {
for (j in 0:len(list_header)) {
if (days[i, list_header[j]] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed.append(consecutive_count)
consecutive_count = 0
}
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 0:length(days)) {
for (j in 0:length(list_header)) {
if (days[i, list_header[j]] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed.append(consecutive_count)
consecutive_count = 0
}
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 0:length(days)) {
for (j in 0:length(list_header)) {
if (days[i]$list_header[j]] == 0) {
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 0:length(days)) {
for (j in 0:length(list_header)) {
if (days[i]$list_header[j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed.append(consecutive_count)
consecutive_count = 0
}
0:length(days)
0:length(days[0,])
0:length(days[,0])
0:nrow(days)
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 1:length(list_header)) {
if (days[i]$list_header[j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed.append(consecutive_count)
consecutive_count = 0
}
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 1:length(list_header)) {
if (days[i,]$list_header[j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed.append(consecutive_count)
consecutive_count = 0
}
days[1,]
days[1,]$week1
a = week1
a = 'week1'
days[1,]$a
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed.append(consecutive_count)
consecutive_count = 0
}
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
sudden_full = c()
for (i in 1:length(day_stayed)) {
if (day_stayed[i] > week_before_homeless) {
if (days[i, 'total_days_stayed'] >= 33) P
sudden_full = c(sudden_full, days[i,])
}
}
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
sudden_full = c()
for (i in 1:length(day_stayed)) {
if (day_stayed[i] > week_before_homeless) {
if (days[i, 'total_days_stayed'] >= 33) {
sudden_full = c(sudden_full, days[i,])
}
}
sudden_full
day_stayed
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
day_stayed = day_stayed[day_stayed>week_before_homeless]
sudden_full = c()
days = days[days$total_days_stayed >= 33]
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
day_stayed = day_stayed[day_stayed>week_before_homeless]
sudden_full = c()
days = days[days$total_days_stayed >= 33,]
sudden_full
days = read.csv("days.csv")
days
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
day_stayed = day_stayed[day_stayed>week_before_homeless]
days = days[days$total_days_stayed >= 33,]
days
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
day_stayed = day_stayed[day_stayed>week_before_homeless]
days = days[days$total_days_stayed >= 33,]
days = days[day_stayed,]
days
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days = days[days$consec_week > week_before_homeless]
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days
days = days[days$consec_week > week_before_homeless]
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days$consec_week
days = days[days$consec_week > week_before_homeless]
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days$consec_week > week_before_homeless
days = days[days$consec_week > week_before_homeless]
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days = days[days$consec_week > week_before_homeless]
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days[days$consec_week > week_before_homeless]
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days = days[days$consec_week > week_before_homeless,]
days = days[days$total_days_stayed >= 33,]
days
days = read.csv("days.csv")
list_header = c('week1', 'week2', 'week3', 'week4', 'week5', 'week6', 'week7', 'week8', 'week9', 'week10', 'week11', 'week12', 'week13', 'week14', 'week15', 'week16', 'week17', 'week18', 'week19')
day_stayed = c()
week_before_homeless = 3 # set week to 3 to represent the time before sudden trainsitions in life
consecutive_count = 0 # count how many consecutive weeks from the initial week that the observation is not in shelters
for (i in 1:nrow(days)) {
for (j in 2:length(list_header)+1) {
if (days[i,j] == 0) {
consecutive_count = consecutive_count + 1
}
}
day_stayed = c(day_stayed, consecutive_count)
consecutive_count = 0
}
days$consec_week = day_stayed
days = days[days$consec_week > week_before_homeless,]
days = days[days$total_days_stayed >= 33,]
days
write.csv(days, file = "sudden_changes.csv")
